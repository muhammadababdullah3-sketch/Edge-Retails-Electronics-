<UserControl x:Class="EdgeRetails.Desktop.Views.Dialogs.ConfirmationDialog"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Border Width="500" Padding="22" Style="{StaticResource Card.Raised}">
  <StackPanel>
   <Grid>
    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold"
               Foreground="{DynamicResource Brush.Text.Primary}"/>
    <Button Content="✕" Command="{Binding CancelCommand}"
            Style="{StaticResource Button.Text.Neutral}"
            IsCancel="True"
            HorizontalAlignment="Right"/>
   </Grid>
   <TextBlock Text="{Binding Consequence}" Margin="0,18,0,0"
              TextWrapping="Wrap" FontSize="13"
              Foreground="{DynamicResource Brush.Text.Secondary}"/>
   <Grid Margin="0,22,0,0">
    <Button Content="Cancel" Command="{Binding CancelCommand}"
            Style="{StaticResource Button.Neutral}"
            IsCancel="True"
            HorizontalAlignment="Left"/>
    <Button Content="{Binding ConfirmText}" Command="{Binding ConfirmCommand}"
            Style="{StaticResource Button.Warning.Compact}"
            IsDefault="True"
            HorizontalAlignment="Right" Height="38" Padding="18,0"/>
   </Grid>
  </StackPanel>
 </Border>
</UserControl>
