<UserControl
    x:Class="EdgeRetails.Desktop.Controls.ModalHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Visibility="{Binding IsOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
    Focusable="False">
    <Grid>
        <Border Background="{DynamicResource Brush.Overlay.Scrim}" />
        <ScrollViewer Margin="24"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalContentAlignment="Center"
                      VerticalContentAlignment="Center"
                      Focusable="False">
            <ContentControl x:Name="ModalPresenter"
                            Content="{Binding Content}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            KeyboardNavigation.TabNavigation="Cycle"
                            KeyboardNavigation.ControlTabNavigation="Cycle"
                            KeyboardNavigation.DirectionalNavigation="Cycle" />
        </ScrollViewer>
    </Grid>
</UserControl>
